import Link from "next/link";
import type { Product } from "@/types/product";
export function ProductCard({ product, isMock = false }: { product: Product; isMock?: boolean }) { return <article className="group overflow-hidden rounded-2xl border border-[#dce5df] bg-white transition hover:-translate-y-1 hover:shadow-xl hover:shadow-[#a85c36]/8"><Link href={`/produits/${product.slug}`} className="block"><div className="relative aspect-[4/3] bg-[#eaf2f2]">{product.image && <div className="absolute inset-0 bg-cover bg-center transition duration-500 group-hover:scale-105" style={{ backgroundImage: `url(${product.image})` }} />}{isMock && <span className="absolute left-4 top-4 rounded-full bg-white px-3 py-1 text-xs font-bold text-[#a85c36]">Données de test</span>}</div><div className="p-6"><p className="text-xs font-bold uppercase tracking-[.14em] text-[#a85c36]">{product.category}</p><h2 className="mt-3 text-xl font-semibold tracking-tight text-[#16232a]">{product.name}</h2><p className="mt-3 line-clamp-2 leading-7 text-[#526259]">{product.short_description}</p><span className="mt-5 inline-block text-sm font-semibold text-[#a85c36]">Voir le produit →</span></div></Link></article>; }
