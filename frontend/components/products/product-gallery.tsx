import type { Product } from "@/types/product";
export function ProductGallery({ product }: { product: Product }) { const images = [...(product.image ? [{ id: 0, image: product.image, caption: product.name, order: 0 }] : []), ...(product.gallery ?? [])].sort((a, b) => a.order - b.order); if (!images.length) return <div className="grid aspect-square place-items-center rounded-2xl bg-[#f1f6f6] p-8 text-center text-sm text-[#526259]">Aucun visuel disponible.</div>; return <div className="grid gap-4 sm:grid-cols-2">{images.map((image) => <figure key={image.id} className="overflow-hidden rounded-2xl bg-[#f1f6f6]"><div className="aspect-square bg-cover bg-center" style={{ backgroundImage: `url(${image.image})` }} /><figcaption className="p-3 text-sm text-[#526259]">{image.caption ?? product.name}</figcaption></figure>)}</div>; }
