import Link from "next/link";
import { RealisationCard } from "@/components/realisations/realisation-card";
import { Container } from "@/components/ui/container";
import { Reveal } from "@/components/ui/reveal";
import { getRealisations, realisationsAreMocked } from "@/lib/realisations";
export async function RealisationsPreviewSection() { const realisations = await getRealisations(); const published = realisations.filter((realisation) => realisation.published).slice(0, 3); return <section id="realisations" className="py-20 sm:py-28"><Container><Reveal className="flex flex-col justify-between gap-5 md:flex-row md:items-end"><div className="max-w-2xl"><p className="text-sm font-bold uppercase tracking-[.18em] text-[#a85c36]">Réalisations</p><h2 className="mt-4 text-3xl font-semibold tracking-tight text-[#16232a] sm:text-4xl">Des fiches d’intervention documentées.</h2><p className="mt-5 leading-7 text-[#526259]">Chaque fiche peut présenter le client, la localisation, les services concernés ainsi qu’une galerie et des comparatifs avant / après.</p></div><Link href="/realisations" className="text-sm font-semibold text-[#a85c36]">Toutes les réalisations →</Link></Reveal>{realisationsAreMocked && <p className="mt-8 text-sm text-[#526259]">Les contenus affichés sont des données de test, en attente de connexion à l’API.</p>}{published.length ? <div className="mt-10 grid gap-6 md:grid-cols-2 lg:grid-cols-3">{published.map((realisation, index) => <Reveal key={realisation.id} delayMs={index * 90}><RealisationCard realisation={realisation} isMock={realisationsAreMocked} /></Reveal>)}</div> : <p className="mt-8 text-[#526259]">Aucune réalisation publiée pour le moment.</p>}</Container></section>; }
