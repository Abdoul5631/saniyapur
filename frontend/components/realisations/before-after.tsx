import type { RealisationImage } from "@/types/realisation";
export function BeforeAfter({ images }: { images: RealisationImage[] }) { const before = images.find((image) => image.type === "before"); const after = images.find((image) => image.type === "after"); if (!before || !after) return null; return <section aria-labelledby="before-after-title"><h2 id="before-after-title" className="text-2xl font-semibold tracking-tight text-[#16232a]">Avant / Après</h2><div className="mt-6 grid overflow-hidden rounded-2xl border border-[#dce5df] sm:grid-cols-2">{[["Avant", before], ["Après", after]].map(([label, image]) => <figure key={label as string} className="relative min-h-72 bg-[#16232a]"><div className="absolute inset-0 bg-cover bg-center" style={{ backgroundImage: `url(${(image as RealisationImage).image})` }} /><div className="absolute inset-x-0 bottom-0 bg-gradient-to-t from-black/70 p-5 text-white"><p className="text-xs font-bold uppercase tracking-[.15em]">{label as string}</p><figcaption className="mt-1 text-sm text-white/80">{(image as RealisationImage).caption}</figcaption></div></figure>)}</div></section>; }
