import type { RealisationImage } from "@/types/realisation";
export function RealisationGallery({ images }: { images: RealisationImage[] }) { const gallery = images.filter((image) => image.type === "gallery").sort((a, b) => a.order - b.order); if (!gallery.length) return null; return <section aria-labelledby="gallery-title"><h2 id="gallery-title" className="text-2xl font-semibold tracking-tight text-[#16232a]">Galerie</h2><div className="mt-6 grid gap-4 sm:grid-cols-2">{gallery.map((image) => <figure key={image.id} className="overflow-hidden rounded-2xl bg-[#16232a]"><div className="aspect-[4/3] bg-cover bg-center" style={{ backgroundImage: `url(${image.image})` }} /><figcaption className="p-4 text-sm text-[#526259]">{image.caption}</figcaption></figure>)}</div></section>; }
